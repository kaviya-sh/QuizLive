spring:
  application:
    name: quizlive

  datasource:
    url: jdbc:postgresql://ep-noisy-brook-aplc69tm.c-7.us-east-1.aws.neon.tech/quizlive_prod?sslmode=require
    username: neondb_owner
    password: npg_Wd59yJgvZpXT
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  data:
    redis:
      host: rational-swan-78267.upstash.io
      port: 6379
      password: gQAAAAAAATG7AAIgcDJlZWU3ODliZjRjNDA0MjM4OTg2YjU2NjM1MjBjMWE3Ng
      ssl:
        enabled: true
      timeout: 60000ms

  mail:
    host: smtp.gmail.com
    port: 587
    username: sparklo.team.in@gmail.com
    password: stqxmobxfzctmdaq
    from: sparklo.team.in@gmail.com
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: ${PORT:8081}
  error:
    include-message: always

jwt:
  secret: the08BskLMecJqFDgVBe%z1euMKeVy3SJqZ6Q781%+m09$1g4fL1CWWXCrPYK6$%fGmkTqTaocvlrGooS8vLQQ==
  expiration-ms: 900000
  refresh-expiration-ms: 604800000

minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin}
  bucket: ${MINIO_BUCKET:quiz-media}

cloudinary:
  cloud-name: Root
  api-key: 436297689376546
  api-secret: n_HqOp9_yYQc05a9ZyWqQGka4vY

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS}

websocket:
  allowed-origins: ${WEBSOCKET_ALLOWED_ORIGINS}

app:
  frontend:
    url: ${FRONTEND_URL}
  google:
    client-id: ${GOOGLE_CLIENT_ID:}
    client-secret: ${GOOGLE_CLIENT_SECRET:}
    redirect-uri: ${GOOGLE_REDIRECT_URI}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: WARN
    com.quizlive: INFO
